version: '3'

services:
  my_service:
    image: python:3.8
    working_dir: /app
    command: sh -c "pip install -r /app/requirements.txt && python /app/run.py"
    volumes:
      - .:/app

    build:
      context: .
      dockerfile: Dockerfile
